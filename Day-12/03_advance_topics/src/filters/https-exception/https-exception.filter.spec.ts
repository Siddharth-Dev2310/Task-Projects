import { HttpsExceptionFilter } from './https-exception.filter';

describe('HttpsExceptionFilter', () => {
  it('should be defined', () => {
    expect(new HttpsExceptionFilter()).toBeDefined();
  });
});
